orm - object relation - mapping
    객체지향언어 - 관계형 디비 - 연결하는프로그램 기법
    django(장고, 파이썬웹프레임워크)
    jpa - java 
    파이썬에서 SQLAlchemy가 지원한다
    전체테이블이 10개미만일때
    테이블 3개이상 조인시 속도문제, 테이블간의 구조가 복잡해질수록
    orm만으로는 처리가 되지않는다.

SPA- single page application,인스타그램,페이스북 등의ㅣ
개발기법, 웹페이지를 하나만 만들어서 페이지 체인지를
하는방식으로 화면이동이 훨씬 매끄럽다. 무한스크롤등을 지원한다.
react(압도적),vuejs(우리나라 금융권),angular,polymer(유튜브)
인스타그램 개발하다가 만든게 react 라이브러리 
spa 방식 웹개발시 orm을 많이 사용한다.

sqlite - 본래의미의 DB는 아니고 사실은 파일이고
        다만 쿼리형식으로 읽고쓰기는 가능하나
        네트워크가 안됨.휴대폰의 전화번호부